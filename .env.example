# Frontend Stripe publishable key (starts with pk_)
VITE_STRIPE_PUBLISHABLE_KEY=pk_your_publishable_key_here

# Backend Stripe secret key (starts with sk_)
STRIPE_SECRET_KEY=sk_your_secret_key_here

# Optional: Server port (defaults to 5000 if not set)
PORT=5000
